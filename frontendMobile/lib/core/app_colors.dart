import 'package:flutter/material.dart';

// AppColors: A central place to manage the app's color palette.
// Using a central class ensures consistency across all screens.
class AppColors {
  static const Color navy = Color(0xFF0E1E3B);  // Primary Brand Color (Navy)
  static const Color gold = Color(0xFF935E28);  // Secondary Brand Color (Gold/Brown)
  static const Color lightGold = Color(0xFFD4AF37); // Accent Gold (used for highlights)
  static const Color white = Color(0xFFFFFFFF);
  static const Color background = Color(0xFFF5F5F5); // Light grey for screen backgrounds
  static const Color inputFill = Color(0xFFFFFEF0); // Subtle yellow for text input fields
  
  // FUTURE IDEA: Add a 'darkBackground' for a potential Dark Mode implementation.
}
