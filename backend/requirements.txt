fastapi>=0.104.0
uvicorn[standard]>=0.24.0
sqlmodel>=0.0.14
psycopg2-binary>=2.9.9
httpx>=0.25.0
pydantic>=2.0.0
python-dotenv>=1.0.0
# Noodsaaklik vir multipart/form-data (lêer-uploads) vanaf die mobiele app.
python-multipart>=0.0.6
# XLSX-import (data-invoer wizard): lees .xlsx werkboeke aan die agterkant.
openpyxl>=3.1.2
Pillow>=10.0.0
# Wagwoord-hashing via passlib (pbkdf2_sha256 — suiwer Python, geen inheemse
# bcrypt C-afhanklikheid in houers nie).
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0,<4.1.0
cryptography>=41.0.0
firebase-admin>=6.0.0
# Survival-model (Phase 2c): RandomSurvivalForest. numpy/scipy/joblib/pandas
# come transitively. sksurv 0.28.0 has cp312 wheels (Docker is python:3.12-slim).
scikit-survival>=0.28.0,<1.0
scikit-learn>=1.9,<1.10
